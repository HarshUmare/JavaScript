//array 

// const myArr = [9, 12, 1, 5, 8]
// console.log(myArr[0]); // 0 based indexing hai

// const newArr = new Array(1, 2, 3, 4, 5)
// console.log(newArr);

/*
 isme to add elements in an array: arr.push(x)
 to remove an element from an array: arr.pop()
*/


// newArr.unshift(9) // 9 ko 0th index mtlb array k start me add krta hai
// console.log(newArr) 
// newArr.shift()
// console.log(newArr) 

// const brr = newArr.join() // converts the array to string
// console.log(brr) // type of brr is String


/*
    SLICE VS SPLICE
    SLICE:
        --> isme last index include nhi hota hai
        --> original array me no change
    
    SPLICE:
        --> isme last index include hota hai
        --> isme original array me changes hai

    // kya changes hote hai kya nhi.... vo slice aur splice use krlo
*/


